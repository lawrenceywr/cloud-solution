{
  "name": "cloud-solution",
  "version": "0.1.0",
  "description": "OpenCode plugin scaffold for cloud and data-center solution design",
  "type": "module",
  "private": true,
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "bun build src/index.ts --outdir dist --target bun --format esm && tsc --emitDeclarationOnly && rm -rf dist/runtime-assets && mkdir -p dist/runtime-assets && cp -R test/converted-markdown dist/runtime-assets/converted-markdown",
    "clean": "rm -rf dist",
    "typecheck": "tsc --noEmit",
    "test": "bun test"
  },
  "dependencies": {
    "@opencode-ai/plugin": "^1.2.24",
    "jsonc-parser": "^3.3.1",
    "zod": "^4.1.8"
  },
  "devDependencies": {
    "bun-types": "1.3.10",
    "typescript": "^5.7.3"
  }
}
